func native printf(format: string, params: any...);

func export main() {
    var str := "this is a test";
    printf("%s\n", str);
}

func factorial(num: int) -> int {
    if(num == 0) return 1;
    return num * factorial(num - 1); 
}
